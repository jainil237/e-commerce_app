{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-syntax": [
      "warn",
      {
        "selector": "CallExpression[callee.name='fetch']",
        "message": "W-08: use the shared RTK Query API (@shared/api) instead of a bare fetch(). Still a warning, not an error — migration is in progress. See workflow/artifacts/tasks/frontend-security-a11y-v1.md before escalating to error."
      }
    ]
  }
}
