# Claude Adapter

Use `CLAUDE.md` when this repository needs Claude-compatible local instructions.

The adapter is intentionally short. It routes the tool to `.workflow/`, where the lifecycle, config, skills, Starter Blocks, and evidence rules live.
